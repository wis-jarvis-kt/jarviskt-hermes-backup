# Cron Job: hermes-self-improve-2am

**Job ID:** cc617d7fd4df
**Run Time:** 2026-05-24 02:01:26
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Review ~/.hermes/SOUL.md, ~/.hermes/config.yaml, and any skill files for outdated info, stale tasks, or improvement opportunities. If changes needed, update files. Report any changes to Master KT. Keep backup of changed files first.

## Response

Good — the `send_reminder.py` script is already correct. The SKILL.md was the only thing out of sync. Let me compile the findings.

---

## Self-Improve Nightly Report — 2026-05-24

### ✅ No Issues Found

**SOUL.md** — Clean, current, no stale references.

**config.yaml** — All settings valid. `display.personality: kawaii` is active. No deprecated fields detected. Config version 23 is current.

**AGENTS.md** — Still accurate. MEMORY.md size note (~22KB) is correctly within the 50K bootstrapMaxChars threshold — no action needed.

---

### 🔧 One Fix Applied

**File:** `~/.hermes/skills/openclaw/remind/SKILL.md`

**Problem:** The WAHA API documentation was wrong — it documented `/api/sendText` with API key auth, but the live system uses `/send` with no auth and a different payload shape. The `openclaw-imports/remind/SKILL.md` (v1.1.0) had the corrected info; the primary `openclaw/remind/SKILL.md` was still on the old endpoint.

**Changes made:**

1. Added proper YAML frontmatter (`name`, `description`, `tags`, `version: 1.2.0`)
2. Fixed WAHA endpoint: `/api/sendText` → `/send`
3. Fixed payload: `{"session":"default","chatId":"...","text":"..."}` → `{"chatId":"...","message":"..."}`
4. Removed hardcoded API key (auth not required for local-only instance)
5. Added verified working curl example
6. Added note: script uses `/send` so direct calls work correctly

**Backups created:**
- `~/.hermes/skills/openclaw/remind/SKILL.md.bak.20250524031044`
- `~/.hermes/workspace/memory/long-term-memory.md.bak.20250524031044`

---

### 📋 Observations (No Action Needed)

- `openclaw-imports/` is a migration staging area from the OpenClaw consolidation — its contents (DESCRIPTION.md: "Skills migrated from an OpenClaw workspace") are intentional and correct.
- `long-term-memory.md` is 9.3KB — well under the 20KB archival threshold noted in AGENTS.md.
- The `send_reminder.py` script was already updated to the correct `/send` endpoint — only the SKILL.md documentation was stale.
- Two copies of the remind skill exist (`openclaw/remind` and `openclaw-imports/remind`) because it was migrated during the OpenClaw consolidation. The primary one (`openclaw/remind`) is the active one used by Hermes.

No other files need updates. System is clean.
