# Cron Job: hermes-self-improve-2am

**Job ID:** cc617d7fd4df
**Run Time:** 2026-06-15 02:02:45
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Review ~/.hermes/SOUL.md, ~/.hermes/config.yaml, and any skill files for outdated info, stale tasks, or improvement opportunities. If changes needed, update files. Report any changes to Master KT. Keep backup of changed files first.

## Response

---

## Changes Made to Master KT

### 🔧 Fix: `apple-notes` built-in skill was stale

**File:** `~/.hermes/hermes-agent/skills/apple/apple-notes/SKILL.md`

**Problem:** The built-in skill still referenced the deprecated `memo` CLI (v1.0.0), which was disabled on this system since 2025-07-27. The user's skill in `~/.hermes/skills/` had already been updated to `osascript` (v2.0.0), but the repo's built-in copy had not been synced.

**Fix:** Replaced the built-in `apple-notes` SKILL.md with the current correct version (osascript-based, version 2.0.0).

**Backup saved:** `~/.hermes/hermes-agent/skills/apple/apple-notes/SKILL.md.bak-20260615`

### ✅ Verified — No Other Issues Found

| Area | Status |
|------|--------|
| `SOUL.md` | Clean — no stale references |
| `config.yaml` | Clean — all config options current |
| `apple-reminders` (built-in) | Already correct — matches user's copy |
| `kanban-orchestrator` (built-in) | Already correct — matches user's copy |
| `kanban-worker` (built-in) | Already correct |
| Cron jobs | All running, last run status OK |
| `research-paper-writing` skill | Present and current |
| Memory logs | Normal daily activity, no stale entries |

### 📝 Note for Future ReviewsThe built-in skills in `~/.hermes/hermes-agent/skills/` and user skills in `~/.hermes/skills/` are **duplicated** — when a user skill gets updated, the built-in copy in the repo doesn't automatically follow. This `apple-notes` discrepancy is the second occurrence (the first was fixed in the user's copy back on 2025-07-27). Consider adding a periodic sync check to the self-improve cron job to catch this class of drift.
