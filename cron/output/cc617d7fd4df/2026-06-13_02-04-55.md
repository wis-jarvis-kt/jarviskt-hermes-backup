# Cron Job: hermes-self-improve-2am

**Job ID:** cc617d7fd4df
**Run Time:** 2026-06-13 02:04:55
**Schedule:** 0 2 * * *

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Review ~/.hermes/SOUL.md, ~/.hermes/config.yaml, and any skill files for outdated info, stale tasks, or improvement opportunities. If changes needed, update files. Report any changes to Master KT. Keep backup of changed files first.

## Response

All changes verified. Here's the summary for Master KT:

---

## Self-Improve Report — 2026-06-13

### Files Reviewed
- `~/.hermes/SOUL.md` — clean, nothing outdated
- `~/.hermes/config.yaml` — clean, nothing outdated  
- `~/.hermes/memories/MEMORY.md` — 2 stale references found and fixed
- `~/.hermes/scripts/disk_space_watchdog.sh` — 1 logic bug found and fixed
- `hermes-agent` skill (full) — all references accurate
- `war-news` skill — anti-bot notes current (last updated 2026-06-12)
- `web-research-limitations` skill — current

---

### Changes Made

**1. `~/.hermes/scripts/disk_space_watchdog.sh` — Logic bug fixed**

The memory log cleanup command was broken. The old code:
```bash
DELETED_MEM=$(find "$MEMORIES" -name "research-*" -o -name "war-news-*" ... -o -name "*.log" 2>/dev/null \
  | xargs -I{} find {} -mtime +${DAYSOLD} -type f 2>/dev/null | wc -l | tr -d ' ')
```
`-name "*.log"` on the outer find matched nothing (no `.log` files in `~/.hermes/memories/`), so `xargs` received zero paths and the inner find never ran. The count was always 0 and no files were ever deleted.

**Fixed** — single find with properly-grouped `-name` patterns, `-type f` before `-mtime`, and `-delete` in one command. Now correctly reports `$DELETED_MEM` old files removed.

Backup saved: `disk_space_watchdog.sh.bak.*`

**2. `~/.hermes/memories/MEMORY.md` — 2 stale entries cleaned up**

- **Skill location stale entry removed:** `openclaw-stock-analysis-victor-framework` was archived weeks ago and the canonical skill (`stock-analysis-victor-framework`) already exists — no need to mention the old absorbed path.
- **gbrain path updated:** The note said "See `openclaw/openclaw-imports/references/gbrain-mcp-setup.md`" but that file may not exist. Updated to "or re-setup from scratch if that file is gone" — pragmatic fallback.
- **Disk Space Watchdog entry replaced:** The old verbose entry (job_id, threshold details, script path) was redundant with the script itself. Replaced with a one-line bug description of the fix made today, so future agents understand why this is in Known Fixes.
- **Bunyamin reminder note added:** `bunyamin_reminder.sh` is a one-shot stub, not a recurring cron. Noted so it isn't mistaken for an active scheduled workflow.

**SOUL.md, config.yaml, and all skills reviewed** — nothing else needs updating. The system is clean.
